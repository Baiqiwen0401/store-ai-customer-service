# 企业微信 AI 客服技术方案

## 1. 目标与范围

本期只交付一个业务闭环：微信客户通过企业微信「微信客服」咨询，系统自动回答门店问题；当消息包含预约意向时，系统创建预约待办并同步企业微信群。

本期支持文本消息、知识直答、大模型辅助、风险转人工、预约待办和群通知。不接入个人微信外挂，不处理支付、退款、发货、小红书或美团渠道，不承诺医疗诊断和疗效。

## 2. 官方接口选型

采用企业微信「微信客服」，不是普通企业微信外部联系人单聊。

- 接收事件：企业微信向 HTTPS 回调地址发送加密的 `kf_msg_or_event` 事件。
- 读取消息：服务端使用事件中的 `Token` 调用 `POST /cgi-bin/kf/sync_msg` 增量拉取正文。
- 回复消息：服务端调用 `POST /cgi-bin/kf/send_msg` 发送文本回复。
- 内部通知：预约待办通过企业微信群机器人 Webhook 推送。
- 身份：使用 `external_userid` 标识客户，使用 `open_kfid` 标识客服账号。

官方约束：客户主动发送消息后 48 小时内最多下发 5 条；客户再次发送消息后重新获得发送额度。接口返回成功不代表最终送达，必须处理 `msg_send_fail` 事件。

## 3. 总体流程

```text
微信客户发消息
  -> 企业微信加密回调
  -> 验签、AES 解密、CorpID 校验
  -> 回调快速返回 success
  -> sync_msg 按 open_kfid + cursor 拉取消息
  -> msgid 幂等去重
  -> 意图识别 / 门店知识 / 大模型 / 风险门禁
  -> send_msg 回复客户
  -> 识别预约意向
  -> 创建 appointment_lead 待办
  -> 异步推送企业微信群
```

## 4. 模块设计

| 模块 | 职责 |
|---|---|
| `WeComCrypto` | SHA1 验签、AES-CBC 解密、PKCS#7 校验、CorpID 校验 |
| `WeComAPI` | 获取并缓存 access_token、调用 `sync_msg`、调用 `send_msg` |
| 微信消息处理器 | 保存游标、按 `msgid` 去重、过滤客户文本、调用客服业务层、记录发送结果 |
| `CustomerService` | 意图识别、知识回答、模型辅助、风险转人工、预约待办 |
| 群通知器 | 新建预约待办后向企业微信群发送摘要，不阻塞客户回复 |

## 5. 数据与幂等

- `channel_cursors`：按租户、渠道、`open_kfid` 保存 `next_cursor`。
- `channel_messages`：保存企业微信 `msgid`、方向、处理状态和错误；唯一约束防止回调重试造成重复回复。
- `customers.external_id`：保存 `wecom_kf:{external_userid}`，复用同一客户。
- `conversations.channel`：固定为 `wecom_kf`，同一客户未关闭会话继续复用。
- `tasks.task_type`：预约使用 `appointment_lead`，同一会话只保留一个待处理预约任务。

## 6. 安全与隐私

- CorpID、Secret、回调 Token、EncodingAESKey、群机器人 Webhook 只通过环境变量注入。
- 回调必须同时通过签名、AES 填充和 CorpID 校验；失败返回 403。
- 日志不得记录 Secret、access_token、手机号全文或原始回调密文。
- 生产地址必须使用 HTTPS；工作台使用正式登录与 RBAC 前不能开放公网管理接口。
- 医疗、过敏、投诉、退款等风险问题继续转人工；AI 回复不作为治疗依据。

## 7. 失败处理

- 回调：先快速确认，再异步拉取，避免企业微信重试。
- 拉取：保存 cursor；`has_more=1` 时继续读取，即使当前 `msg_list` 为空。
- access_token：内存缓存并提前刷新；平台返回鉴权错误时清缓存重试一次。
- 模型超时：常见问题继续使用本地知识；复杂问题创建人工待办，不阻塞回调。
- 群通知失败：不影响客户回复，记录错误供重试和监控。
- 发送失败事件：关联原发送 `msgid`，更新状态并进入告警/人工处理。

## 8. 开发阶段

1. 企业微信协议层：加密回调、access_token、`sync_msg`、`send_msg`。
2. 业务接入层：客户/会话映射、cursor 持久化、消息幂等、AI 回复。
3. 预约通知：待办摘要、企业微信群通知、重复通知控制。
4. 自动化验证：密码学、回调、分页拉取、幂等、发送失败和原有 36 条评测。
5. 真实联调：配置企业微信后台、HTTPS 回调、客服账号、应用权限和群机器人。
6. 生产加固：PostgreSQL、Redis/任务队列、监控告警、备份恢复、RBAC、灰度发布。

## 9. 用户需要提供

- 已认证或可使用「微信客服」的企业微信组织及管理员权限。
- 企业 ID（CorpID）。
- 一个自建应用及其 Secret，并将该应用配置为「微信客服-可调用接口的应用」。
- 微信客服账号，设置为通过 API 管理；对应接待人员处于应用可见范围。
- 回调 Token 和 43 位 EncodingAESKey。
- 企业微信群机器人 Webhook。
- 一个有域名和有效 HTTPS 证书的公网部署地址。

以上凭据不得发送到 GitHub；本地联调时通过系统环境变量配置。

## 10. 验收标准

- 企业微信后台成功验证回调 URL。
- 客户文本消息仅处理一次，10 秒内收到知识直答；复杂问题超时后有明确兜底。
- 同一微信客户复用客户和未关闭会话。
- 预约消息只创建一个待办，并在内部群收到一次通知。
- 重复回调不会重复回复或重复通知。
- 发送失败事件可以在数据库和日志中追踪。
- 所有密钥不进入代码、Git、响应或日志。
